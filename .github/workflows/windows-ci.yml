name: Optional CI Windows

on: [push]

jobs:
  build: 
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [windows-2016, windows-2019]
          
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: mkdir build && cd build && cmake .. && cmake --build .
